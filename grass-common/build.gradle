
apply plugin: 'idea'

dependencies {
    compile "io.netty:netty-all:4.0.30.Final"
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: mavenLocalUrl)
        }
    }
}