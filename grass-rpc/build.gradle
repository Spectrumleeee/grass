apply plugin: 'idea'

version = '1.0.0'

dependencies {
    compile project(":grass-remote")
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: mavenLocalUrl)
        }
    }
}