apply plugin: 'idea'

dependencies {
    compile project(":grass-common")
    
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: mavenLocalUrl)
        }
    }
}